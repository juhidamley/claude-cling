# claude-cling

A JetBrains plugin that watches integrated terminal tabs for Claude Code runs and:

- plays a cling/beep when Claude Code appears to finish
- shows a warning cling when Claude Code prints text that looks like it needs attention

## Development

```bash
./gradlew test buildPlugin
```
